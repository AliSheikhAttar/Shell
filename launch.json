{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with Console",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "console": "integratedTerminal"  // This enables stdin input
        }
    ]
}
